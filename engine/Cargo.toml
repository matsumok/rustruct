[workspace]
members = [
    "rustruct-types",
    "rustruct-calc",
    "rustruct-wasm",
]
resolver = "2"

[workspace.dependencies]
serde = { version = "1", features = ["derive"] }
nalgebra = "0.34"
wasm-bindgen = "0.2"
tsify-next = "0.5"
