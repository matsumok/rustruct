# 解析設定はSeismicModelと分離するが、独立エンティティとしては永続化しない

SeismicModel(質量・層間剛性・階高)と解析設定(減衰h・入力地震動)はドメイン上は別概念だが、解析設定を名前付きで使い回せる独立テーブルにはしない。実行のたびに指定し、使用した設定はCalcResultのJSONBに結果と一緒に埋め込んで保存する。テスト段階で設定の使い回し機能はオーバースコープであり、埋め込み保存にすれば「どの設定でこの結果が出たか」の再現性を常に確保できるため。設定の使い回しニーズが顕在化したら独立テーブル化を再検討する。
