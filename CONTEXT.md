# Rustruct

構造計算(地震応答解析)Webツール。建物のせん断型多質点モデルに対して地震応答解析を行う。

## Language

**SeismicModel**:
解析対象となる建物の構造モデル。せん断型多質点系(自由度=階数)・線形を前提とする。モデル名と、最下層から順に並ぶStoryの列を持つ。階数はStoryの数として導出され、独立した値としては持たない。減衰や入力地震動などの解析条件は含まない。
_Avoid_: 建物、構造物(モデルという意味で使うときはSeismicModelを指す)

**Story**:
SeismicModelを構成する1つの層。質量(t)・層間剛性(kN/m)・階高(m)を1階分まとめて持つ。SeismicModel内のstoriesは最下層(1F)を先頭(index 0)として下から上へ並び、階番号はこの位置から導出する — 独立したフィールドとしては持たない(並びと矛盾した番号が存在できてしまうため)。飛び番・地下階はv1のスコープ外。
_Avoid_: 階・層をコード上の型として指すときはStoryに統一(FloorやLevelは使わない)

**解析設定**:
SeismicModelを解析する際に指定する条件。減衰定数(h、モデル全体で一律)と入力地震動(標準波形セットからの参照)からなる。名前付きで独立保存・再利用はしない — 実行のたびに指定し、使用した内容はCalcResultに埋め込んで保存する。
_Avoid_: 解析条件、パラメータ

**入力地震動**:
解析に用いる地震波形データ。v1では告示波などの標準波形セット(既往3波)をアプリケーションに同梱した固定データとし、解析設定はそこから参照して指定する。ユーザーによる独自波形のアップロードはv1のスコープ外。
_Avoid_: 地震波、入力波(解析設定の一部として扱う場合はこの用語を使う)

**CalcResult**:
SeismicModelに解析設定を適用して得られる解析結果。再現性のため、計算に使った解析設定(減衰・入力地震動)を結果本体と一緒に保持する。

**層間変形角**:
層間の相対変位を階高で除した値。SeismicModelには含まれず、CalcResultの導出値として算出される。
